[pytest]
testpaths = tests
# Ensure tests that create files don't pollute the project root
# Tests should use fixtures like tmp_path or explicitly manage working directory
tmp_path_retention_count = 0
tmp_path_retention_policy = none
markers =
    smoke: mark test as smoke test
    regression: mark test as regression test